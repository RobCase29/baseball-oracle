{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run db:deploy && npm run archive:register-catalog:remote && npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/admin/*.ts": {
      "maxDuration": 300
    }
  }
}
